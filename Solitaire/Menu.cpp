#include "Menu.h"
#include <thread>
#include <chrono>

Menu::Menu()
{
}


Menu::~Menu()
{
}

void Menu::openMenu()
{

}

void Menu::closeMenu()
{

}

void Menu::init()
{

}

void Menu::newGame() //Creates a new game
{

}

void Menu::continueGame() //Continues from the previous game
{

}

void Menu::openSettings() //Opens the settings menu
{

}

void Menu::openStatistics() //Opens the player's statistics
{
	cout << "Haha, you suck" << endl;
}

void Menu::openCredits() //Yeah... banter?
{
	cout << "Hello" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "Welcome to my slow scrolling credits!!" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "There is no escape :)" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "Please, do not fear it will all be over soon." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "Just Kidding.." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "There is no escape..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "Haha! Got you!" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "I would like to credit myself!" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "I mean I did put a lot of effort into this!" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "Don't you just love slow scrolling credits that you can't escape?" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "Anyways, please hire me" << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "..." << endl; this_thread::sleep_for(chrono::seconds(1));
	cout << "Thanks for watching!" << endl; this_thread::sleep_for(chrono::seconds(1));

}